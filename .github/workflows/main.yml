name: pedidos-fast-food-pipeline

on:
  push:
      branches:
        - main

jobs:
  build:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: '20'
    - run: npm ci

  deploy-aws:
    runs-on: ubuntu-latest

    if: github.ref == 'refs/heads/main'
    environment:
      name: Prod
    needs: build

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Configure AWS credentials
        id: aws-credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: us-east-1
          role-to-assume: arn:aws:iam::103447461627:role/3soat-fiap-role

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1
        with:
          aws-region: us-east-1
          role-to-assume: arn:aws:iam::103447461627:role/3soat-fiap-role

      - name: Deploy Image To AWS
        id: build-image
        env:
          aws-region: us-east-1
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: pedidos-ms
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          echo "::set-output name=image::$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"
      # When we want to deploy to the k8s cluster we can just uncomment the following
      # - name: Configure kubectl
      #   uses: azure/k8s-set-context@v1
      #   with:
      #     method: kubeconfig
      #     kubeconfig: ${{ secrets.KUBE_CONFIG_DATA }}

      # - name: Deploy to EKS
      #   run: |
      #     kubectl apply -f ./k8s/values-production.yaml

